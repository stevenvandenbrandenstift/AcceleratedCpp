#include <iostream>

int main()
{
	// fixed by putting * in ' '
	/*this is a comment that extends over several lines
	because it uses '*'/ and /'*' as its starting and ending deimiters
	*/

	std::cout << "Does this work?" << std::endl;

	return 0;
}
